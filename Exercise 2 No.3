#include <stdio.h>

int main() {
    int n, triangularNumber;
    for (n = 5; n <= 50; n += 5) {
        triangularNumber = n * (n + 1) / 2;
        printf("%d ", triangularNumber);
    }
    printf("\n");
    return 0;
}
